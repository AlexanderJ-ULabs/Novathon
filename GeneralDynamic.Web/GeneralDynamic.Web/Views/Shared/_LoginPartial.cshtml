@using Microsoft.AspNetCore.Identity
@using GeneralDynamic.Web.Models
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@if (SignInManager.IsSignedIn(User))
{
    <form asp-area="" asp-controller="Account" asp-action="Logout" method="post" id="logoutForm" class="navbar-right">




        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <a href="#Link" class="dropdown-toggle" data-toggle="dropdown">
                        <i class="material-icons">dashboard</i>
                        <p class="hidden-lg hidden-md">Dashboard</p>
                    </a>
                </li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle btn-rotate" data-toggle="dropdown">
                        <i class="material-icons">notifications</i>
                        <span class="notification notification-count" style="display: none"></span>
                        <p class="hidden-lg hidden-md">

                            <b class="caret"></b>
                        </p>
                    </a>

                    <ul class="dropdown-menu notification-list">
                        @*<li>
                                <a href="#">Mike John responded to your email</a>
                            </li>
                            <li>
                                <a href="#">You have 5 new tasks</a>
                            </li>
                            <li>
                                <a href="#">You're now friend with Andrew</a>
                            </li>
                            <li>
                                <a href="#">Another Notification</a>
                            </li>
                            <li>
                                <a href="#">Another One</a>
                            </li>*@
                    </ul>
                </li>
                @if (Context.User.IsInRole("Administrator"))
                {
                    <li>
                        <a asp-area="" asp-controller="Pages" asp-action="Accounts">
                            <i class="fa fa-wrench fa-fw"></i>Account

                        </a>
                    </li>

                }
                @*<li>

                        <a href="#link" class="dropdown-toggle" data-toggle="dropdown">
                            <i class="fa fa-user fa-fw" aria-hidden="true"><span id="UserLoggedInId" style="display: none">@UserManager.GetUserId(User)</span> </i><span id="UserLoggedIn">@UserManager.GetUserName(User)</span>
                        </a>

                    </li>*@
                <li>
                    <a href="#Link" class="dropdown-toggle" data-toggle="dropdown" onclick="document.getElementById('logOut').click();">
                        <i class="fa fa-power-off"></i>

                    </a>
                </li>

                <li>
                    <button type="submit" id="logOut" style="display:none" class="btn btn-info navbar-btn navbar-link"></button>
                </li>



                <li class="separator hidden-lg hidden-md"></li>
            </ul>

        </div>

    </form>
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li><a asp-area="" asp-controller="Account" asp-action="Register">Register</a></li>
        <li><a asp-area="" asp-controller="Account" asp-action="Login">Log in</a></li>
    </ul>
}